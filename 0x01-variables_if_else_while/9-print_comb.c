#include <stdio.h>
/**
 * main - prints all possible combination of single digit nimbers
 *
 * Return ; 0 on on success
 * int main(void)
 */
int i = '0' ;
while (i < = '9' )
         {
		 putchar (i);
	 if (i ! = '9')
	 }++ i ;
putchar('\n');
return (0);
}
