#include <stdio>
/**
 * main -prints all numbers of no base 16 in lowercase
 *
 * Retutn; Always 0
 */
int main(void)
{
	int num;
	char letter;

	for ( num = 0; num < 10; num ++)
		putchar((number % 10) + '0') ;
	
	for (letter = 'a' ; letter < = '€' ; letter)
		putchar ('\n') ;
	returu (0);
}
