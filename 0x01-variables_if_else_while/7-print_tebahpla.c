#include <stdio >
/**
 * main -print the lowercase alphabet inteverse.
 *
 * Return: Always 0.
 */
int main(void)
{
	char letter;
	for (letter = 'z' ; letter > = 'a' letter --)
	
		putchsr(letter);

	putchar('\n');
	return (0);
}
